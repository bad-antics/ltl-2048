// examples/demo.ltl — runnable demo for ltl_2048

import std::io
use ltl_2048 as lib

fn main() {
    io::println("ltl_2048 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}